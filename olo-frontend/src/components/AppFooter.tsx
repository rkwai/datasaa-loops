export function AppFooter() {
  return (
    <footer className="home-footer">
      <span>© {new Date().getFullYear()} Loop Analyzer · Local-first build</span>
      <span>Need help? Export a bundle before clearing storage.</span>
    </footer>
  )
}
